/* pages/user/myGarden/myGarden.wxss */
.card {
  position: relative;
  margin: 20rpx 20rpx;
  border: 2rpx solid #000;
  overflow: hidden;
}

.card-title-bg {
  width: 100%;
  height: 80rpx;
  position: absolute;
  background-color: #000;
  opacity: 0.6;
  bottom: 0;
}

.card-title {
  position: absolute;
  color: #fff;
  padding: 15rpx 0 15rpx 30rpx;
  bottom: 0;
}

/* 分页 */
.page{
  padding: 30rpx 0 !important;
}